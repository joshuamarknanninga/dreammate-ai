# dreammate-ai
# DreamMate AI — MVP

## Run it locally

1) Backend
- Copy `.env.example` to `server/.env` and fill keys.
- Install & seed:
  ```bash
  cd server
  npm i
  npm run db:seed
  npm run dev
